# Proxy Server Command

Start a local HTTP proxy server for debugging network traffic.

## Usage

```bash
nns proxy [OPTIONS]
```

## Options

*To be documented when implemented*

## Examples

*To be documented when implemented*

```bash
# Start proxy on default port
nns proxy

# Start proxy on custom port
nns proxy --port 8080

# Start proxy with request logging
nns proxy --port 8080 --log requests.log
```

## Technical Details

*To be documented when implemented*
